{
  "name": "cf-poc-104",
  "private": true,
  "version": "0.0.0",
  "description": "PoC: Cloudflare Worker connect() TCP socket -> 104:8443 (drives the all-Cloudflare feasibility decision)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
